pyve_schema = "3.0"

[project]
name = "modelfoundry"

[env.root]
purpose = "utility"
backend = "none"
languages = ["python"]
frameworks = ["none"]

[env.testenv]
purpose = "test"
default = true
backend = "micromamba"
languages = ["python"]
frameworks = ["pytest", "ruff", "mypy"]
