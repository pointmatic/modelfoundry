# Copyright (c) 2026 Pointmatic
# SPDX-License-Identifier: Apache-2.0
#
# PyTorch recipe with an Optuna optimization stage (TPE + median pruning).
schema_version: 1
plugin: pytorch
seed: 7
Data:
  recipe: dr_recipe.yml
Architecture:
  type: resnet20
  num_classes: 3
  in_channels: 3
Loss:
  op: cross_entropy
Optimizer:
  op: adamw
  learning_rate: 0.01
Training:
  max_epochs: 2
  batch_size: 8
  device: cpu
  precision: fp32
  checkpoint_cadence: 1
Optimization:
  sampler: tpe
  pruner: median
  n_trials: 4
  max_epochs_per_trial: 2
  search_space:
    Optimizer.learning_rate:
      log_uniform: [1.0e-4, 1.0e-2]
  baseline_trial: enqueue_recipe_defaults
Evaluation:
  splits: [val]
  primary_metric: accuracy
  metrics: [accuracy, macro_f1, confusion_matrix]
  calibration_bins: 10
