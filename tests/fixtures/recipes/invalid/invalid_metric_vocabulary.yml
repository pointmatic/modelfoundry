# Copyright (c) 2026 Pointmatic
# SPDX-License-Identifier: Apache-2.0
#
# check 11: metric not in the pre-production vocabulary
schema_version: 1
plugin: pytorch
seed: 7
Data:
  recipe: dr_recipe.yml
Architecture:
  type: resnet20
  num_classes: 3
  in_channels: 3
Loss:
  op: cross_entropy
Optimizer:
  op: adamw
  learning_rate: 0.01
Training:
  max_epochs: 2
  batch_size: 8
  device: cpu
  precision: fp32
  checkpoint_cadence: 1
Evaluation:
  splits:
  - val
  primary_metric: accuracy
  metrics:
  - accuracy
  - bogus_metric
  calibration_bins: 10
